/**
 * Shared position/size for the dashboard home avatar and the drawer avatar,
 * so the drawer's avatar sits exactly where the home avatar fades out from —
 * opening the drawer reads as the home avatar being "revealed" underneath,
 * not two separate elements.
 */
export const AVATAR_SIZE = 46;
export const AVATAR_IMAGE_SIZE = 33;
export const AVATAR_LEFT = 13;
export const AVATAR_TOP_OFFSET = 19;
